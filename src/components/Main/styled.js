import styled from '@emotion/styled';

const Main = styled.main`
  align-items: center;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  margin: 0 auto;
  max-width: 1200px;
  padding: 40px 20px;
`;
Main.displayName = 'Main';

export default { Main };
